name: CI

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      
      - name: Install dependencies
        run: yarn
      
      - name: Start project
        run: yarn start:dev
